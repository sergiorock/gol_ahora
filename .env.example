# Linux: copiar a .env y ajustar con tus IDs reales
#   cp .env.example .env
#   id -u   → pegar en USER_ID
#   id -g   → pegar en GROUP_ID
#
# Mac / Windows con Docker Desktop: no hace falta nada, los defaults funcionan.

USER_ID=1000
GROUP_ID=1000
