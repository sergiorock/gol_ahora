<% content_for :page_title, "Editar — #{@entrenamiento.nombre}" %>
<% content_for :header_actions do %>
  <%= link_to admin_entrenamiento_path(@entrenamiento), class: "btn btn-secondary" do %>
    <i class="ti ti-arrow-left"></i> Volver
  <% end %>
<% end %>

<%= render "form", entrenamiento: @entrenamiento %>
