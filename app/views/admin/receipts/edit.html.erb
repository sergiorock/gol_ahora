<% content_for :page_title, "Editar recibo #{@receipt.receipt_number}" %>
<% content_for :header_actions do %><%= link_to admin_receipt_path(@receipt), class: "btn btn-secondary" do %><i class="ti ti-arrow-left"></i> Volver<% end %><% end %>
<div class="row justify-content-center"><div class="col-lg-7"><div class="card"><div class="card-body"><%= render "form", receipt: @receipt %></div></div></div></div>
